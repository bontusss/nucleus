meta {
  name: Tenant Login
  type: http
  seq: 1
}

post {
  url: {{baseURI}}tenant/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ikwecheghu@gmail.com",
    "password": "password",
    "tenant_id": 1
  }
}

tests {
  const body = res.body;
  bru.setEnvVar("token", body.data.token);
  
}

settings {
  encodeUrl: true
}
